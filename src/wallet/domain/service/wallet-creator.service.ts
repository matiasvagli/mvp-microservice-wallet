/* class WalletTypeClassifier {
  classifyByAge(age: number): WalletType {
    if (age >= 18) return WalletType.standard();
    if (age >= 13) return WalletType.teen();
    throw new Error("Users under 13 cannot have wallets");
  }
} */